# 맨투맨 (M2M) — AI 진로 멘토링 서비스

> **"직무 정보 접근성의 불평등은 개인의 노력 부족이 아니라, 연결·식별·발화·자산화라는 네 단계의 구조적 막힘에서 비롯된다."**

막연한 진로 고민을 구조화하고, 기존 멘토 답변 자산으로 답변 가능한지 검증한 뒤, 필요한 경우 적합한 현직자 멘토에게 연결하며, 새 멘토 답변을 다시 자산화하는 **AI 진로 멘토링 서비스 구현 프로젝트**입니다.

3개의 판단형 Agent와 1개의 자산화 파이프라인으로 구성된 기능 단위 MVP입니다.

---

## 📌 문제 정의

### 채용 구조의 전환이 만든 정보 비대칭

채용시장이 공채 중심에서 직무 중심 수시채용으로 전환되며(89.8%, KDI 2025), 기업은 직무 이해도와 실무 경험을 중심으로 선별하는 구조로 변화했다. 이 변화는 지원자에게 새로운 요구를 부과했다 — 특정 직무가 실제로 무엇을 하는지, 어떤 준비가 필요한지를 **스스로 탐색하고 이해해야 한다**는 것이다.

그러나 그 탐색에 필요한 현직자 경험은 개인 네트워크에 따라 접근성이 크게 달라진다. 대학생의 46.9%가 진로를 결정하지 못한 상태이며(잡코리아 2021), 한국직업능력연구원 대학 진로교육 현황조사(2024)에서도 구체적 취업정보 수집 어려움과 실무경험 부족이 반복 보고된다.

이것은 **정보의 절대량 문제가 아니라 접근 경로의 불평등 문제다.**

### 사회적 자본의 분배 불평등

동일한 대학생이라도 가족·선배·지인 네트워크를 통해 현직자와 연결되는 경우와 그렇지 못한 경우 사이에는 직무 정보 획득 기회에 상당한 차이가 존재한다. 이는 개인의 노력 차이가 아니라 **사회적 자본(social capital)의 분배 불평등**이다. 부르디외(Bourdieu)의 자본 이론으로 보면, 경제적 자본과 달리 사회적 자본은 가시화되지 않기 때문에 격차가 구조적으로 재생산된다.

직무 중심 채용은 학생에게 더 많은 자기주도적 탐색을 요구하지만, 그 탐색에 필요한 자원은 균등하게 분배되어 있지 않다. 따라서 맨투맨은 단순한 정보 제공 서비스가 아니라, **현직자 경험으로 가는 경로를 AI가 매개하는 구조**를 목표로 한다.

### 구조적 차단의 4단계

현직자 경험에 도달하는 과정은 다음 네 단계에서 반복적으로 막힌다.

| 단계 | 막히는 지점 | 기존 서비스의 한계 |
|------|------------|-------------------|
| ① **현직자 접근** | 가족·지인 네트워크에 없으면 연결 불가 | 잇다·코멘토 등은 인기 멘토 폭주 / 나머지 비대칭 |
| ② **멘토 식별** | 어떤 기준으로 적합한 현직자를 고를지 알 수 없음 | 멘티에게 "찾는 책임" 부과 — 식별 역량 자체가 정보 자본 |
| ③ **질문 설계** | 고민을 구체적 질문으로 정리하지 못한 채 대화 시작 | 막연한 질문이 정제 없이 멘토에게 전달 → 양쪽 비효율 |
| ④ **경험의 자산화** | 1:1 답변은 한 멘티에게만 전달되고 사라짐 | 매년 같은 질문 반복, 멘토 부담 누적 |

---

## 🗺️ 문제 구조와 서비스 대응

| 구조적 막힘 | 서비스 대응 | 구현 요소 |
|------------|------------|---------|
| 고민을 질문으로 정리하지 못함 | 멀티턴 대화로 질문 정제 | Agent 1 |
| 기존 답변으로 답할 수 있는지 판단 어려움 | 검색 후 관련성·근거충분성·상황적합성 검증 | Agent 2 |
| 적합한 현직자를 고르기 어려움 | 멘티 맥락과 멘토 프로필 기반 Top-3 추천 | Agent 3 |
| 1:1 답변이 사라짐 | 멘토 답변을 임베딩·태그와 함께 자산화 | Assetization |

---

## 🔄 서비스 흐름

```
사용자: "데이터 직무가 궁금한데 비전공자라 어디서 시작해야 할지 모르겠어요"
        │
        ▼
┌─────────────────────────────────────┐
│  STEP 1. 질문 정제 (Agent 1)         │
│                                     │
│  현재 상태 / 관심 직무 / 보유 경험    │
│  제약 조건 / 알고 싶은 내용 수집      │
│                                     │
│  멀티턴 대화 → 정보 충분성 자기 평가  │
│  → 정제 질문 + 구조화 메타데이터 산출 │
└──────────────────┬──────────────────┘
                   │
                   ▼
        ┌──────────────────┐
        │  세션 생성         │
        │ (question_sessions)│
        └────────┬─────────┘
                 │
                 ▼
┌─────────────────────────────────────┐
│  STEP 2. 검색·검증 (Agent 2)         │
│                                     │
│  전략 판단: search_first / mentor_first
│       ↓                             │
│  임베딩 검색 (자산 DB)               │
│       ↓                             │
│  5기준 검증: 관련성 / 근거 충분성 /  │
│             최신성 / 상황 적합성 /   │
│             개인정보 안전            │
└──────┬──────────────┬───────────────┘
       │              │
  score 높음      score 낮음
       │              │
       ▼              ▼
┌────────────┐  ┌──────────────────────┐
│ AI 직접 답변│  │  STEP 3. 멘토 매칭    │
│(자산 재구성)│  │  (Agent 3)           │
└────────────┘  │                      │
                │  3채널 임베딩 유사도   │
                │  → 규칙 필터          │
                │  → 품질 평가 (루프)   │
                │  → LLM Top-3 선별    │
                └──────────┬───────────┘
                           │
                           ▼
               ┌───────────────────────┐
               │  STEP 4. 멘토 답변 수신│
               │  STEP 5. 자산화        │
               │                       │
               │  임베딩 생성 → DB 저장  │
               │  다음 멘티에게 재활용   │
               └───────────────────────┘
```

---

## 🤖 왜 Multi-Agent Architecture인가

처음에는 LLM이 진로 질문에 직접 답하는 챗봇 형태도 가능하다고 생각했다. 그러나 실제 멘토링 질문 50건을 검토하면서 문제는 단순 답변 생성이 아니라는 점을 확인했다.

하나의 질문 안에 정보 탐색, 개인 상황 판단, 자료 피드백 요청, 멘토 경험 요청이 섞여 있었다. 단일 LLM 호출로는 **질문이 충분히 정제됐는지, 기존 답변으로 답해도 되는지, 어떤 멘토가 적합한지, 답변을 어떻게 재사용할지**를 분리해서 판단할 수 없다.

따라서 각 판단을 분리한 multi-agent 구조를 선택했다. 이 구조에서 각 단계의 책임은 명확하게 분리되고, 검색 가능한 질문은 자산화 답변으로 처리하며, 개인 맥락이 강한 질문은 멘토 매칭으로 넘긴다.

맨투맨의 목표는 LLM이 현직자를 대체하는 것이 아니다. **멘토링 자원이 필요한 질문을 더 정확히 선별하고, 반복되는 질문은 자산화된 답변으로 효율적으로 처리하는 것**이다.

---

## 🧩 Agent 설계

### Agent 1 — 질문 정제 (`agents/question_refine_agent.py`)

**역할:** 막연한 진로 고민을 멘토가 답변하기 좋은 구체적 질문으로 변환한다.

멘티가 질문 설계 능력을 갖추지 않아도 현직자에게 전달 가능한 형태로 만들어주는 것이 핵심이다.

**에이전트 루프**

```
[관찰] 현재까지 수집된 5개 항목 확인
   → 현재_상태 / 관심_직무 / 보유_경험 / 제약_조건 / 알고_싶은_내용
[판단] 필드별 가중합 충분성 검사 (≥ 0.75)
[행동 A] 부족 → 빠진 항목에 대한 추가 질문 생성
[행동 B] 충분 → REFINEMENT_PROMPT + 자기검증(CHECK_PROMPT) → 구조화 출력
```

**구조화 출력 (단순 정제 질문을 넘어)**

- `question_units`: 복합 질문을 독립 단위로 분리
- `current_bottleneck`: 멘티가 막힌 지점을 표준 표현으로 분류 (14개 라벨)
- `hard_case_flags`: requires_artifact_review / recency_sensitive / scope_too_broad
- `routing_hints`: search_strategy_hint / source_role / target_role / bridge_hypothesis
- `taxonomy_tags`: 도메인·직무·질문 유형·스킬 태그

이 구조화 결과가 Agent 2·3의 판단 기준으로 전달된다.

---

### Agent 2 — 검색·검증 (`agents/search_verify_agent.py`)

**역할:** 기존 자산화 답변으로 답할 수 있는지 판단하고, 가능하면 AI가 직접 답변한다.

단순 RAG와의 차이는 검색된 답변을 무조건 사용하지 않는다는 점이다. 관련성, 근거 충분성, 상황 적합성, 최신성, 개인정보 안전성을 검증한 후 분기한다.

**에이전트 루프**

```
[관찰1] hard_case_flags 선처리
   → requires_artifact_review=True → 즉시 멘토 연결 (검색 생략)
[판단] 전략 결정: search_first / mentor_first
[행동1] 임베딩 검색 → 코사인 유사도 상위 5개 후보
[행동2] 5기준 검증 (LLM 판단, 점수 계산은 코드)
[관찰3] 점수 구간 판단
   → score ≥ threshold : llm_direct
   → mid ≤ score < threshold : partial_with_mentor_suggest
   → score < mid : mentor_needed
```

**임계값 설계**

| 전략 | 직접 답변 기준 | 부분 답변 기준 | 최소 유사도 |
|------|--------------|--------------|-----------|
| search_first | 0.65 | 0.45 | 0.50 |
| mentor_first | 0.75 | 0.50 | 0.45 |

mentor_first는 개인 맥락이 강한 질문이므로 더 높은 품질 기준을 요구한다.

---

### Agent 3 — 멘토 매칭 (`agents/mentor_match_agent.py`)

**역할:** 멘티의 질문과 배경을 바탕으로 적합한 멘토 Top-3를 추천한다.

단순 직무 유사도 매칭이 아니라 `current_bottleneck`, `source_role`, `target_role`, `bridge_hypothesis`, `transferable_skills`를 활용해 멘티가 막힌 지점을 가장 잘 해결할 수 있는 멘토를 선별한다. 멘티에게 "누구를 찾아야 할지"까지 요구하지 않는다.

**에이전트 루프 (최대 2회)**

```
[시도 1] min_sim = 0.30
   Step 1: 3채널 통합 임베딩 유사도 (프로필 + 경험 + 기존 답변)
   Step 2: 규칙 필터 + 도메인·병목·전환 보너스
   Step 3: 재정렬 (유사도×0.55 + 만족도×0.20 + 활동량×0.10 + 수용가능×0.15)
   Step 4: 상위 후보 추출
[관찰] 품질 평가 (후보 수 < 3 or 평균 점수 < 0.40 → 부족)
[판단] 부족 → min_sim = 0.15로 완화 후 재시도
[행동] LLM Top-3 선별 + 추천 근거 생성
```

---

### Assetization — 답변 자산화 (`agents/assetize.py`)

**역할:** 멘토의 1:1 답변을 다음 멘티에게 재사용 가능한 답변 자산으로 저장한다.

매번 같은 질문이 반복되어 멘토 부담이 커지는 문제를 구조적으로 줄인다. 자산화된 답변은 Agent 2의 검색 대상으로 즉시 편입된다.

```
멘토 답변 수신
    → 이중 동의 확인 (멘토 + 멘티)
    → 품질 판단: 개인맥락 강도 / 정보 밀도 / 시의성 / 기밀성
    → 통과 시: 임베딩 생성 + 메타데이터 태그 생성 → DB 저장
    → 다음 유사 질문 시 Agent 2의 검색 대상으로 포함
```

답변이 누적될수록 AI 직접 답변 비율이 높아지고 멘토에게 전달되는 질문은 개별 맥락이 중요한 케이스로 좁혀진다. **사용이 늘수록 서비스 품질과 운영 효율이 동시에 개선되는 자기강화 구조**다.

---

## 🔬 개발 과정에서의 검증

AI 출력물을 그대로 신뢰하지 않고, 어떤 판단이 안정적이고 어떤 판단이 어려운지 직접 확인했다.

### Agent 1 평가 설계

실제 멘토링 질문 50건을 분석해 Agent 1 설계 가이드라인을 만들었다. 그중 복합질문, 직무전환, 최신트렌드, 자료피드백, 가능성판단 등 판단이 까다로운 12개 케이스를 선별해 사람이 직접 gold annotation을 달았다.

`eval/eval_runner.py`가 Agent 1의 `analyze()` 메서드를 single-shot으로 호출하고, 출력 결과를 gold와 자동 비교한다.

**평가 항목 및 가중치**

| 평가 항목 | 가중치 | 점수 | 해석 |
|----------|--------|------|------|
| privacy_safe | 5% | **1.00** | PII 제거 안정적 |
| hard_case_flags | 20% | **0.92** | 자료검토 필요·최신성·위험 플래그 감지 안정적 |
| routing | 15% | 0.64 | mentor_first 판단은 강하나 search_first 분기 개선 여지 확인 |
| refined_question | 5% | 0.58 | 전체 방향은 맞지만 일부 세부 요소 누락 |
| current_bottleneck | 20% | 0.42 | 유사 병목 라벨 간 혼동 |
| taxonomy_tags | 10% | 0.26 | domain은 잡지만 role/question_type 태그가 약함 |
| question_units | 25% | 0.29 | 복합 질문을 하나로 압축하는 경향 |
| **전체 평균** | | **0.54** | |

**검증을 통해 확인한 것**

Agent 1은 위험 감지와 개인정보 보호에는 안정적으로 작동한다. 반면 복합 질문을 독립 단위로 분해하는 것과 유사한 병목 라벨 간 정확한 분류는 더 어려운 문제임을 확인했다.

이 결과를 바탕으로 Agent 2·3에서는 Agent 1의 구조화 결과를 확정값으로 사용하기보다, 검색 검증과 멘토 매칭 단계에서 다시 한 번 안전하게 분기하도록 설계했다. 또한 프롬프트 개선 1회 시도 후 동일 eval셋 재평가를 통해 개선 방향의 효과를 검증했으며, 같은 평가셋으로 반복 개선하는 것이 과적합(overfitting)을 유발한다는 판단 하에 1회로 종료했다.

Agent 2(검색·검증)와 Agent 3(멘토 매칭)은 외부 DB 품질에 의존하는 구조여서 현재 초기 버전에서는 자동 정량 평가보다 설계 원칙과 로직 검증을 우선했다. 실데이터가 누적된 이후 정량 평가를 추가할 예정이다.

---

## 📁 프로젝트 구조

```
m2m-proto/
├── agents/
│   ├── question_refine_agent.py   # Agent 1: 질문 정제 (멀티턴 + 충분성 루프 + analyze())
│   ├── search_verify_agent.py     # Agent 2: 검색·검증 (임베딩 검색 + 5기준 평가)
│   ├── mentor_match_agent.py      # Agent 3: 멘토 매칭 (품질 평가 + 재시도 루프)
│   └── assetize.py                # 자산화 파이프라인
│
├── db/
│   └── json_db.py                 # JSON 파일 기반 CRUD 유틸
│
├── utils/
│   └── embedding.py               # text-embedding-3-small 임베딩 / 코사인 유사도
│
├── data/
│   ├── generate_personas.py       # 멘티 50명 + 멘토 50명 페르소나 생성
│   ├── seed_real_cases.py         # 실제 잇다 Q&A 50건 → mentor_answers.json 씨딩
│   └── guidelines/                # 50건 분석 산출물 (패턴 노트, 가이드라인)
│
├── eval/
│   ├── eval_agent1_gold.json      # Agent 1 gold annotation (12 hard cases)
│   ├── eval_runner.py             # 자동 평가 실행기
│   └── eval_results_agent1.json   # 평가 결과 (케이스별 점수)
│
├── json_db/
│   ├── mentees.json               # 멘티 장기 프로필
│   ├── mentors.json               # 멘토 기본 정보
│   ├── question_sessions.json     # 세션별 Agent 1 구조화 출력
│   ├── mentee_experiences.json    # 멘티 경험 이력
│   ├── mentor_experiences.json    # 멘토 경험 이력
│   └── mentor_answers.json        # 자산화된 멘토 답변 + 임베딩
│
├── main.py                        # 전체 흐름 실행 진입점
├── .env                           # OPENAI_API_KEY (git 제외)
├── requirements.txt
└── README.md
```

---

## ⚙️ 실행 방법

### 환경 설정

```bash
pip install -r requirements.txt
```

`.env` 파일 생성:
```
OPENAI_API_KEY=sk-...
```

### 전체 플로우 실행

```bash
python main.py
```

메뉴에서 `1`을 선택하면 Agent 1 → Agent 2 → (필요 시) Agent 3 순서로 실행된다.

### Agent 1 평가 실행

```bash
# 12개 전체 케이스 평가
python eval/eval_runner.py

# 단일 케이스 테스트
python eval/eval_runner.py --case EVAL_A1_001
```

### 페르소나 생성

```bash
python data/generate_personas.py --replace        # 멘티 + 멘토 50명 교체 생성
python data/generate_personas.py --only mentee    # 멘티만
python data/generate_personas.py --only mentor    # 멘토만
```

### 초기 자산 씨딩

```bash
python data/seed_real_cases.py --replace
```

---

## 🛠️ 기술 스택

| 분류 | 도구 |
|------|------|
| LLM | GPT-4o (질문 정제 / 검증 / 매칭 판단) |
| 임베딩 | text-embedding-3-small (OpenAI) |
| 유사도 | Cosine Similarity (numpy) |
| DB | JSON 파일 기반 (경량 로컬 저장소) |
| 언어 | Python 3.11 |

---

## 📊 현재 데이터 현황

`generate_personas.py`는 멘티·멘토 각 50명 단위의 페르소나 생성을 지원한다. 아래 표는 10개 도메인별 대표 시드 구성을 요약한 것이다.

| # | 도메인 | 멘토 | 멘티 |
|---|--------|------|------|
| 1 | 데이터 분석 / AI | 3명 | 3명 |
| 2 | 마케팅 | 3명 | 3명 |
| 3 | 컨설팅 | 3명 | 3명 |
| 4 | 금융 / 투자 | 3명 | 3명 |
| 5 | IT 개발 | 3명 | 3명 |
| 6 | UX / 디자인 | 3명 | 3명 |
| 7 | HR / 조직문화 | 3명 | 3명 |
| 8 | 서비스 기획 | 3명 | 3명 |
| 9 | 미디어 / 콘텐츠 | 3명 | 3명 |
| 10 | 비영리 / 사회적기업 | 3명 | 3명 |

**초기 자산:** 자산화 답변 50개 (10개 도메인 × 5개 시나리오, 임베딩 포함)

---

## 🔭 확장 방향

맨투맨의 구조는 데이터가 누적될수록 자기강화된다.

멘토 답변이 쌓일수록 Agent 2가 더 많은 질문을 기존 자산으로 처리할 수 있다. 반복되는 질문은 자산화 답변으로 처리하고, 개별 맥락이 강한 질문만 멘토에게 전달된다. 멘토 부담은 줄고, 멘티는 더 빠른 답변 경험을 얻는다.

**사용자 축:** 비수도권 대학생 → 비전공 직무 전환자 → 경력단절자 → 시니어 재취업자

**기관 축:** 대학 진로개발센터 → 지자체 청년정책 부서 → 고용노동부 청년진로 사업

확장이 가능한 이유는 맨투맨이 특정 직무 정보를 직접 제공하는 것이 아니라, **경험을 가진 사람과 그 경험이 필요한 사람 사이의 접근 경로 비대칭을 매개하는 구조 자체를 서비스**하기 때문이다.

---

## 📚 참고 문헌

- KDI (2025). 채용 트렌드 분석: 수시채용 전환 현황
- 잡코리아 (2021). 대학생 진로 결정 현황 조사
- 한국직업능력연구원 (2024). 대학 진로교육 현황조사
- Bourdieu, P. (1986). *The Forms of Capital*. In J. Richardson (Ed.), Handbook of Theory and Research for the Sociology of Education.

---

작성일: 2026-06-23 | 개발자: 박수영 (취정사)
